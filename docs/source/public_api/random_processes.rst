probnum.random_processes
========================

Random Processes.

Random or stochastic processes generalize functions by encoding uncertainty over the
function in its covariance function. Random processes can be used to model deterministic
functions which are not fully known.

.. automodapi:: probnum.random_processes
	:no-heading:
